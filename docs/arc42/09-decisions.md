# Decisions

See `docs/adr/` for decisions on disabled production constructs. Summary: the interview demo values private Bedrock invocation, auditability, and deterministic evals while deferring fixed-cost managed retrieval and reranking until production scale justifies them.
