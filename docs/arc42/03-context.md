# Context

The system sits between a user/API client and public benefits documents. Trust boundaries are the HTTP API boundary, model provider boundary, object/audit storage boundary, and AWS VPC endpoint boundary. It is a no-PHI demonstration of payer-style controls rather than a production claims system.
